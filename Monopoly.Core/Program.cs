using Monopoly.Core.Data;
using Monopoly.Core.Models;

namespace Monopoly.Core
{
    internal class Program
    {
        static void Main(string[] args)
        {
            //foreach (var chance in CardSet.GetChanceCards()) {
            //    Console.WriteLine(chance.Info);
            //}

            //foreach (var street in CardSet.GetStreetCards()) 
            //{
            //    Console.WriteLine(street.UKName);
            //}
        }
    }  
}
